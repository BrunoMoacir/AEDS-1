#define MAX_lIN 2
#define MAX_COL 4
void leituraMatriz(M[MAX_lIN][MAX_COL]){
    for(int i = 0; i < MAX_lIN; i++){ // percorre as linhas
        for(int j = 0; j < MAX_COL; i++) //percore as colunas
    }
}