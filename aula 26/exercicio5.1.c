#define MAX_lIN 2
#define MAX_COL 4

void verificaMaior(M[MAX_lIN][MAX_COL], int i){
    int linhaEscolhida;

    printf("\n Escolha uma linha de 0 a %d", i-1);
    scanf("%i", %linhaEscolhida);

    for(int j = 0; j < MAX_COL; j++){
        matriz[linhaEscolhida][j] > maior{
            maior = matriz[linhaEscolhida][j];
        }
    }
        printf("O maior valor da linha %d eh: %.1f\n", linhaEscolida, maior);
}