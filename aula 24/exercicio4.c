void lerVogais (char* str[][100], int n){
    for(int i = 0; i < n; i++){
        int contador = 0;
        for(int j = 0; str[i][j] != '/0'; j++){
            char c = string[i][j];
            if (c == 'a'|| c == 'e'|| c == 'i' || c == o || c == 'u' ){
            [count ++]
            }
        }
    }
    printf("string %d tem %d vogais\n", i+1,contador);
}